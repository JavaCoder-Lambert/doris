# 4.1.4 NULL-safe Join 修复与交付记录

核查日期：2026-09-11。源码目录 `doris-4.1.4-nullsafe`，分支 `codex/4.1.4-null-safe-join-fix`。本文记录本 fork 的修复；原版发行包对照见 [验证记录](null-safe-join-validation.md)，跨版本升级条件见 [升级评估](4.1.4-upgrade-notes.md)。

## 已提交的修复

基线为官方 4.1.4 的 `ad35a140c7fd0b842f18c23300bac581f7d04326`，修复提交为 [`523e9146c1ffd305edae46bb1bbbf3546d248163`](https://github.com/JavaCoder-Lambert/doris/commit/523e9146c1ffd305edae46bb1bbbf3546d248163)。它按 4.1.4 现有实现回补上游 [#65975](https://github.com/apache/doris/pull/65975) 的必要 BE 变更，没有整段替换 master 的实现。

`ColumnNullable` 用 null map 表示 SQL NULL，其内部字符串列仍可能保留非空字节。原版单字符串 Hash Join 的 key 生成没有使用调用方传入的 null map，导致 NULL 行带着残留字节进入查找，错过应有的 NULL 匹配。修复在 build/probe 两侧把 NULL 行规范为默认 `StringRef()`；真正的空字符串仍按原有 NULL 标记和哈希表逻辑区分。

| 文件 | 变更 |
|---|---|
| [hash_map_context.h](../../be/src/exec/common/hash_table/hash_map_context.h) | 将外部 null map 传入 `MethodStringNoCache::init_serialized_keys_impl`，规范 NULL 行；保留 4.1.4 聚合的 `_sub_table_groups` 行为 |
| [string_ref.h](../../be/src/core/string_ref.h) | 长度为零时直接判断相等，避免将默认空引用的空指针传入 `memcmp` |
| [hash_table_method_test.cpp](../../be/test/exec/hash_map/hash_table_method_test.cpp) | 覆盖 String32/String64、build/probe、nullable 包装/嵌套输入、NULL/空串区分、输入不被修改及聚合路径 |
| [string_value_test.cpp](../../be/test/runtime/string_value_test.cpp) | 覆盖默认空引用与真实空串的对称相等判断 |
| [官方 SQL suite](../../regression-test/suites/query_p0/join/test_null_safe_eq_join_string_key.groovy)及 [golden](../../regression-test/data/query_p0/join/test_null_safe_eq_join_string_key.out) | 原样移植上游提交 `e5b67e095b4fd40acd656ad25aed6bf5d8b52718` 的输入和预期 |

这项补丁修复的是已经复现的**单字符串 NULL 匹配结果错误**。目前没有复现用户六键业务查询的历史 BE crash；截图中的 `block_null_safe_equal` 是 FE 规则拦截。3.0.8 的相关 nullable Join 使用不同的 key 选择路径，因此没有把这个新版本补丁机械回补到 3.0.8。

第二项修复已提交并推送为 [`8048d149bd8264ce72c919ace2d6319aebe0597d`](https://github.com/JavaCoder-Lambert/doris/commit/8048d149bd8264ce72c919ace2d6319aebe0597d)，按 4.1.4 实现适配上游 [#67755](https://github.com/apache/doris/pull/67755)：

| 文件 | 变更 |
|---|---|
| [vdata_stream_sender.h](../../be/src/exec/exchange/vdata_stream_sender.h) | 每次 Exchange RPC 创建独立 callback、Controller 和 response，避免回调重入时下一次发送覆盖仍在处理的完成状态 |
| [brpc_closure.h](../../be/src/util/brpc_closure.h) | 在进入业务回调前释放已完成请求的 attachment，包括弱回调已销毁的情况；保留本分支的 QueryContext 和 RPC 错误处理钩子 |
| [exchange_sink_test.cpp](../../be/test/exec/exchange/exchange_sink_test.cpp) | 四项回归覆盖 response/controller 重入、释放先后顺序和回调销毁后的释放 |
| [hash_table_method_test.cpp](../../be/test/exec/hash_map/hash_table_method_test.cpp) | fixture 使用 const 列视图读取共享子列，保留原有输入和断言，修正测试自身的可写 COW 访问错误 |

这项 RPC 修复与 `<=>` key 算法独立。四项确定性回归已从失败变为通过，但没有把它认定为用户历史生产事故的根因。最终 Release 构建锁定源码提交 `8048d149bd8264ce72c919ace2d6319aebe0597d`；后续仅文档提交不改变该安装包的来源身份。

## 已实际执行的基线

| 测试 | 原版 3.0.8 | 原版 4.1.4 | 本 fork x86_64 构建与验收 |
|---|---|---|---|
| 同组 100 项 SQL 探针 | 100 通过 | 80 通过，20 结果错误 | 待构建后运行 |
| 其中六键查询三种形态 × 五种 RF 设置 | 15 通过 | 15 通过 | 待运行 |
| 60 项扩展字符串探针 | 60 通过，计划与健康检查通过 | 32 通过，28 结果错误；60 项计划检查全部通过 | 待运行 |
| 官方 `test_null_safe_eq_join_string_key` | 未运行 | 首个 `minimal_left_join` 断言失败：预期 `[2,1]`，实际 `[2,NULL]` | 待运行 |
| 定向 BE 单测 / ASAN | 未运行 | 未运行 | 字符串补丁 + 未修 RPC 的基线：5 通过、4 项 RPC 断言失败、2 项 fixture COW 异常；修复后 11/11 通过，0 跳过 |

以上原版发行包实验均使用 ARM64 二进制；不能将其写成 x86_64 修复包通过。4.1.4 官方回归框架与 Java UDF 已用本机 jenv JDK 17.0.19 编译成功，随后连接原版包完成红测；框架编译不生成修复后的 BE。

扩展探针使用固定种子、256 行 probe、40 行常规 build、33 行全 NULL build、空 build，`batch_size=16`。七种形态分别在 RF OFF / GLOBAL BLOOM、Broadcast / Shuffle 下失败：单侧 CAST 的 INNER/LEFT、单侧 CONCAT 的 INNER/LEFT、两侧 CONCAT、全 NULL build 的 INNER/LEFT。NULL 常量、两键、非对称 Nullable、NULL 与真实空串等其他组合通过。完整范围见 [扩展探针说明](../../tools/null-safe-join/EXTENDED.md)。

扩展实验的 FE/BE 都报告 `doris-4.1.4-rc04-ad35a140c7f`；BE 前后 Alive，`LastStartTime` 不变；容器未发生 OOM 或自动重启。EXPLAIN 证明实际选择的 Join 分发与 RF 生成，尚不证明 RF 运行时到达/过滤行数，也不证明具体算子的内部块数。

随后在精确 `doris-3.0.8-rc01-09b0cc49a6` 上运行相同脚本：60/60 通过，无变量兼容错误或计划覆盖降级。两轮 fixture 摘要、场景顺序及全部 60 项独立预期逐项一致；BE 测试前后存活，版本与启动时间未变。加上原有 100 项，这个版本在已执行的两组共 160 项检查中全部通过；这仍不替代缺失的生产事故复现。

本地证据位于仓库旁 `../doris-investigation-20260911/`，不提交测试数据和构建产物：

- `official-regression-4.1.4-baseline.md`：框架命令、两项 Maven 构建成功与官方 suite 红测。
- `results/nullsafe_ext_414_baseline_20260911.json`、`.sql`：全部 60 项查询、计划、独立预期摘要、缺失/额外行及节点状态。
- `results/nullsafe_ext_308_baseline_20260911.json`、`.sql`：精确 3.0.8 的同组扩展对照。
- `results/extended414-containers-before-stop.txt`：扩展测试完成后的进程、OOM 与重启计数。
- `results/probe-3.0.8-complete.json`、`results/probe-4.1.4-complete.json`：同组 100 项对照。

## 构建和发布状态

已准备隔离的 Linux x86_64 构建环境；不修改宿主机默认 Java 8。最终包必须来自本 fork 同一提交的 FE/BE 构建输出，并记录实际版本、编译模式、CPU 选项、依赖、源码 SHA 和包摘要。此时尚无通过验收的安装包，不能用原版 BE 配合新源码或单独编译成功的回归框架冒充修复包。

Linux 独立 FE 构建已在 2026-09-11 14:44（北京时间）实际退出 `0`，27 个 Maven 模块全部构建成功。它来自提交 `523e9146c1ffd305edae46bb1bbbf3546d248163`，编译了 5,148 个 FE core 生产源码与 1,317 个测试源码；命令使用 `-DskipTests`，测试未执行。生成的 `doris-fe.jar` 为 33,209,380 字节，SHA-256 为 `9100111f53247941018246dec849afb3f0938a805948eb7b90fbb0f6fcac7ecb`，包含 UI 静态文件。这个独立 FE 输出只证明该阶段构建成功，不能与后续不同提交的 BE 混成最终交付包。

该 Linux 构建使用 JDK 17.0.2。其默认 `ProcessBuilder` 启动方式曾在运行 protobuf 编译器前报 `Failed to exec spawn helper`；独立小程序也能以 `/bin/true` 复现，直接执行同一 protobuf 编译器则通过。只为本次 Maven 进程设置 `MAVEN_OPTS='-Xmx4g -XX:MaxMetaspaceSize=1g -Djdk.lang.Process.launchMechanism=FORK'` 后，官方 FE 构建的 protobuf 生成和全部后续阶段成功，未修改 Doris 构建源码或全局 Java 配置。详细命令、失败与成功日志保存在本地 `../doris-build-state/build-execution-addendum.md`。

Linux x86_64 ASAN 定向测试程序已于北京时间 15:24 编译、链接成功，ELF 为 2,847,338,880 字节，SHA-256 为 `efb089f1517439d24b41a5e136f1051d3d030249f0692e7d109986ca20481c2c`。这份保留的基线程序包含已提交的字符串修复与四项 RPC 回归测试，其中的 RPC 生产实现未打补丁。原始 BE 构建依赖、ASAN 和链接库保留，只缩小了测试对象文件范围。

在 Mac 上隔离的 Rosetta Linux 环境中，程序执行 `--gtest_list_tests` 即退出 `139`，没有产生 GTest XML。旧 glibc 远程调试器曾停在后台采样线程调用的固定 `vsyscall` 地址；独立小程序在无调试器时调用同一地址却成功，因此这一调试信号尚不能当作原始启动失败的根因。换用 glibc 2.35 后仍出现启动失败。这些启动失败不是 RPC 断言红测，也不是用户生产事故的复现。原始日志、二进制身份和独立环境探针保存在本地 `../doris-build-state/logs/ut-focused-baseline-v2/`、`ut-debug-container/`、`ut-modern-runtime/` 与 `ut-modern-debug/`。

随后使用任务独立的 QEMU 全系统 x86_64 Linux 虚拟机，未改变上述 ASAN ELF；两个 AVX2 探针、正常 ASAN 探针及预期报错的 use-after-free 探针均符合预期，程序成功列出准确 11 项测试。北京时间 17:10 完成首次真实断言运行：11 项全部执行，5 通过、6 失败，进程退出 `1`，无 ASAN 错误，guest OOM 增量为零。四项 RPC 测试分别复现旧 response/controller 被下一次 RPC 污染、回调入口附件未释放、弱回调销毁后附件未释放。另两项字符串测试在进入 key 断言前因 fixture 读取共享列时选中可写接口而抛出 COW 异常；已改为 const 列视图，未修改输入或预期。

真实 XML 的 SHA-256 为 `c043b3519bb546fecfdb24f5e49e52208cc7018f32183e05cc1a22f7f8a9577c`，完整证据保存在 `../doris-build-state/x86-system-vm/logs/full-x86-baseline-v2/`。这四项 RPC 红测验证回调重入与资源释放契约，不等于复现用户的历史生产 crash。

北京时间 18:46:59，修复后的 focused ASAN 程序在同一完整 x86_64 Linux guest 中完成 GREEN：8 项 Exchange、2 项 NULL key 标准化、1 项 StringRef 测试，共 **11/11 通过，0 跳过、0 失败**。test/reporter/wrapper 均退出 `0`，测试阶段没有 sanitizer 报告，guest vmstat 与 cgroup OOM kill 增量均为零。两个 AVX2 与正常 ASAN 探针通过，故意 UAF 的负向探针产生预期报告并退出 `134`。

GREEN ELF 为 2,847,339,176 字节，SHA-256 `d9bbeb639af5fac0a19c77ad5155d67fb4f9c8484d633ddc847c69abc3f53c36`；实际构建身份为 `523e9146` 加四文件 v4 补丁，补丁 SHA-256 `7c766759bc7747c35b9ba17a072850ad5322f6323b1add85bc2a6dbec1a93ffa`。随后逐字节核对该 BE 差异与已提交的 `8048d149` 完全一致，未把测试 ELF 中原有构建版本号改写为最终提交。原始 GREEN XML SHA-256 `2a64d52ad66d10733449661c567028056fa820bfc541c894bd98c9a965ebc4bd`，归档 SHA-256 `cbc5ee70692a824c3d62429fb7fce3c79aeb8be5578c9e03d32cf73bbc3193bc`，证据在 `../doris-build-state/x86-system-vm/logs/full-x86-green-v4b/`；源码对应关系见相邻 `green-commit-receipt.json`。这不是全量 BE 测试通过，也不是最终 Release 安装包或物理 Intel/AMD 服务器的运行验收。

`clang-format 16` 仓库检查已通过。上游 golden 的 SHA-256 为 `275b54aa5d50469638067d8bac3abe98db00209a058090e9237df6205da01c0c`，suite 为 `4c270bbc9ce1ccc9994dcfe74d1e9a4b6ebadf5c1676d22888035447daf79014`。golden 保留上游生成格式的末尾空行，Git 默认 EOF 空白检查会提示该行；其余代码空白检查通过，未为消除提示改写预期。

生产升级仍需完整 `q_raw`、实际字段类型、代表性数据、多 BE 场景及恢复演练。现有证据能界定这项已复现缺陷，不能保证所有未来 Join/RF 问题不存在。
